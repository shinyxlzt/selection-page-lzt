
  # Section Selection Page

  This is a code bundle for Section Selection Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  `npm run build`
  `npm run deploy`